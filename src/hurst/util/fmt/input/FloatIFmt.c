#include "FloatIFmt.h"

const struct FloatIFmt FLOAT_IFMT_DEFAULT = {
    .allowSign              = true,
    .allowSpacesBeforeSign  = true,
    .allowSpacesBeforeInt   = true,
    .allowSpacesAfterInt    = true,
    .allowSpacesBeforeFract = true,
    .allowSpacesAfterFract  = true,
};
